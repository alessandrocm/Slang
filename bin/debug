#!/usr/bin/env node-debug

var server = require('./../app');
var port = process.env.PORT || 3001;

server.listen(port, function(){
  console.log('grand-slacker-station (DEBUG MODE) listening on port ' + port);
});
